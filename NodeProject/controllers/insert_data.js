var data=require('../view/js/Marketplace');
var db_connection = require('../model/db_connection');

const products_added= items;
console.log(products_added);

connection_object.query('insert into asset_expoit_map values(',{asset_type:'Computer',exploit:'DOS',risk_level:2}, function(err,response){
    if(err) throw err;

    console.log('Insert successful');
});

